import { Box } from "@mui/material";
import { colors } from "../../data/constant";

const NoInput = () => {
  return (
    <Box m={1} mx={2} color={colors.grey.grey1}>
      -
    </Box>
  );
};

export default NoInput;
